Como iniciar el servidor?

 - En el directorio DjangoApp\myProject\ hay que usar este commando con anaconda:
     <python manage.py runserver>
     
 - Para parar el servidor hay que pulsar Ctrl + C en la consola de miniconda
